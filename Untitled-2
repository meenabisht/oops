class User {

    __constructor() {

        $this->database = new PDO('mysql://dajfs:dsaf');
    }

    public login($email, $password) {
        $user = $this->database->prepare('select * ')
        return $data;
        return FASLE;

    }

    register($emial, $pass, $dob) {
        $this->database = 
    }


}


$user = new User();

if ($data = $user->login($_POST['email], $_POST['password])) {
    $_SESSIon = $data;
}
else echo message;
